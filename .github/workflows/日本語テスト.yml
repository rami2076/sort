name: 日本語テスト.
on:
  push:
    branches-ignore:
      - master
jobs:
  日本語ジョブ名:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Event name is ${{ github.event_name }}."
      - run: echo "Brnach name is ${{ github.ref }}."
